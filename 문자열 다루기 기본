class Solution {
    public boolean solution(String s) {
        boolean answer = true;
        int result;
        try { result = Integer.parseInt(s); 
            } catch (NumberFormatException e) { result = 0; } 
        if (result == 0) {
            answer = false;
        } 
        return answer;
    }
}
